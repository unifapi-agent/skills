interface:
  display_name: "Web Page Change Monitor"
  short_description: "Plan public page change monitoring before crawl, snapshot, diff, and alert APIs exist."
  default_prompt: "Use $web-page-change-monitor to define target pages, expected diffs, proxy evidence, and the crawl/diff APIs required."
