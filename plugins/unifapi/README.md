# UnifAPI Plugin Bundle

This directory is a generated Codex marketplace bundle.

Edit the root source files instead:

- `.codex-plugin/`
- `.mcp.json`
- `skills/`
- `docs/`

Then run:

```bash
node scripts/build
```

Use `node scripts/build --check` to verify that generated bundles have not drifted from the root source.
